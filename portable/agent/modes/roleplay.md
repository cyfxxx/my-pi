# 角色扮演模式 — 人设（占位）

> 此文件由 `portable/agent/modes.json` 的 `roleplay.appendPrompt` 指向，
> 经 `pi --append-system-prompt` 追加到系统提示词。请在下方填写人设。

## 人设

（待填写：角色设定、语气、称呼、边界等。）

## 约定

- 本模式的长期记忆存放在独立命名空间 `portable/memory/roleplay/`，与正常记忆隔离。
- 仅启用 `web-search` 与 `memory` 两个功能；不启用自动化/子代理/上下文压缩等功能。
