# Runtime data
*.pid
*.lock
*.json
*.jsonl
!package.json
!vitest.config.ts
!tsconfig.json
!config/*.json

# Scripts (runtime helpers, not source)
scripts/
# 例外：随仓库分发的语音服务脚本（faster-whisper / sherpa-onnx 后端；fresh checkout 需要）
!features/voice/scripts/
!features/voice/scripts/**

# Test artifacts
tests/__mocks__/

# Logs
*.log
