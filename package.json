{
  "name": "my-pi-root",
  "private": true,
  "workspaces": [
    "custom"
  ],
  "scripts": {
    "build:vendor": "cd vendor/pi && npm install && npm run build",
    "build:custom": "cd custom && npm run build",
    "build": "npm run build:vendor && npm run build:custom",
    "dev": "./scripts/dev.sh",
    "clean": "npm run clean --workspaces"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
